package com.sleepy.main;

import java.sql.SQLException;
import java.util.List;

import com.sleepy.sql.DBOperator;

public class Test {

//	public void test() {
//		try {
////			DBOperator operator = new DBOperator();
////			operator.connectDB();
////			operator.useDB();
//			// operator.displayData();
//			// operator.addData(new String[] {"101", "grape","fruits","9.0","99"});
////			List<String> result = operator.serach("category", "fruits");
////			for (String s : result) {
////				System.out.print(s + "\t");
////			}
//			System.out.println();
//			operator.disconnectDB();
//		} catch (SQLException e) {
//			// TODO Auto-generated catch block
//			e.printStackTrace();
//		}
//	}

	public void old() {
		// RepertoryM manager = new RepertoryM();
		// manager.connectDB();
		// manager.init();
		// manager.deleteData(1);
		// manager.updateData(new Goods(2, "Carrot", "Vegetables", (float) 21.5));
		// manager.updateData(new Goods(3, "Grape", "Fruits", (float) 5.7));
		// manager.deleteData(new Goods(4, "Beans", "Vegetables", (float) 4.8));
		// manager.alterColumn("quantity INT NOT NULL");
		// manager.deleteData(2);
		// manager.deleteData(3);
		// manager.addData(new Goods(1, "Apple", "Fruits", (float) 1.5,54));
		// manager.addData(new Goods(2, "Carrot", "Vegetables", (float) 21.5,12));
		// manager.addData(new Goods(3, "Grape", "Fruits", (float) 5.7,11));
		// manager.addData(new Goods(4, "Beans", "Vegetables", (float) 4.8,75));
		// manager.updateCharDataByID("2", "name", "Beans");
		// manager.updateCharDataByID("4", "name", "Carrot");
		// manager.updateNumDataByID("3", "quantity", "33");
		// manager.displayData();
		// manager.disconnectDB();
		//
		// for(int i=5;i<101;i++) {
		// SqlManager.getInstance().addData(new Goods(i, "Apple"+i, "Fruits", (float)
		// (1.5+i),54+i));
		// }

	}
}
